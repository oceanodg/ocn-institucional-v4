import { Inter, Lora } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  variable: "--font-sans",
});

export const lora = Lora({
  subsets: ["latin"],
  variable: "--font-serif",
});
